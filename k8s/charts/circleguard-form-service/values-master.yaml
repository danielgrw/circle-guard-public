# Master: mirror Stage registry/repo pattern — pipeline passes `--set image.tag=<7-char-sha>` via deployToNamespace.
image:
  repository: docker-registry:5000/circleguard-form-service
  tag: "master"
  pullPolicy: Always

service:
  port: 8086

env: master
replicaCount: 2
resources: {}
