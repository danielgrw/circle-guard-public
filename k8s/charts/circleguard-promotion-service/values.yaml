replicaCount: 1

image:
  repository: circleguard-promotion-service
  tag: "changeme"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8088

env: dev
resources: {}

autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
